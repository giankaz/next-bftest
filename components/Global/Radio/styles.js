import styled from 'styled-components'

export const StyledDiv = styled.div`
  width: 100%;

  display: flex;
  align-items: center;

  margin-bottom: 10px;
 
    .span__read {
      font-size: 13px;
      color: var(--grey2);
      padding:0 10px 0 0;
      width: 100%;
    }

    .error {
      font-size: 13px;
      color: var(--red);
      padding:0 10px 0 10px;
    }
`